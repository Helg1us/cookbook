interface:
  display_name: "Commit"
  short_description: "Create safe atomic git commits"
  default_prompt: "Use $commit to create focused git commits from the current changes."
policy:
  allow_implicit_invocation: false
